<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.selfroom.mappers.SeatInformationMapper">
    <select id="getSeatInformation" resultType="com.example.selfroom.bean.SeatInformation">
        SELECT * FROM test.seatinformation  WHERE roomNumber = #{room}
    </select>
</mapper>