<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.selfroom.mappers.BuildDataMapper">
    <select id="getBuildData" resultType="com.example.selfroom.bean.BuildData">
        SELECT * FROM test.buliddata  WHERE roomFloor = #{floor}
    </select>
</mapper>